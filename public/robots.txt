# robots.txt for Concussion Education Australia
#
# NOTE: do NOT add per-bot "Allow: /" groups for AI crawlers. Under the
# Robots Exclusion Protocol a crawler obeys ONLY its most-specific matching
# group, so a named group containing just "Allow: /" cancels every Disallow
# below for that bot (this exposed /p/ prospect pages to AI crawlers until
# 2026-07-10). AI search/citation bots (GPTBot, OAI-SearchBot, ClaudeBot,
# PerplexityBot, Applebot, DuckAssistBot, Google-Extended, ChatGPT-User,
# anthropic-ai, cohere-ai) are welcome and governed by the * group.

User-agent: *
Allow: /

# Disallow admin/private areas
# Bare paths (no trailing slash) so the rule covers /dashboard as well as /dashboard/...
Disallow: /api/
Disallow: /dashboard
Disallow: /admin
Disallow: /dev-login
Disallow: /direct-access
Disallow: /auth/
Disallow: /login
Disallow: /settings
Disallow: /checkout
Disallow: /learning
Disallow: /modules/
Disallow: /pay/
# Custom per-prospect pitch pages + partner pages — private, never index
Disallow: /p/
Disallow: /d/
Disallow: /partners/
Disallow: /preseason/b/
Disallow: /proposals/
Disallow: /ep-pitch/
Disallow: /sst-pitch/
Disallow: /reference/success
# Pre-launch gated app surfaces (SST Trainer platform + demo entry links)
Disallow: /platform
Disallow: /demo/
Disallow: /sst-trainer
Disallow: /clinical-testing
Disallow: /clinical-hub
Disallow: /join-clinic
# Private review / ops surfaces
Disallow: /ep-course
Disallow: /outreach-kit
Disallow: /talk
Disallow: /admin-workflow
Disallow: /courses/poll
Disallow: /s/
Disallow: /upgrade
Disallow: /verify
# Pre-launch hidden tools suite (Screen Comfort + future on-device eye tools).
# EXCEPTION: /tools/bctt-calculator is a deliberately PUBLIC, INDEXABLE clinical
# calculator (see its page docstring + self-canonical + sitemap entry). Google
# honours the most specific matching rule, so this Allow wins for that one URL
# while everything else under /tools/ stays blocked. Without it the page carried
# a self-canonical on a Disallow-ed URL — the "indexed, though blocked" state.
Allow: /tools/bctt-calculator
Disallow: /tools/
Disallow: /screen-comfort
# Research & publications — hidden until the first DOIs land
Disallow: /publications

# Training-only crawlers with no search/citation value for AU clinicians —
# blocked (Bytespider was 60% of AI traffic; pure content extraction).
User-agent: Bytespider
Disallow: /

User-agent: CCBot
Disallow: /

# Sitemap location
Sitemap: https://portal.concussion-education-australia.com/sitemap.xml

# LLM-readable information
# https://portal.concussion-education-australia.com/llms.txt
# https://portal.concussion-education-australia.com/llms-full.txt
# https://portal.concussion-education-australia.com/.well-known/llms.txt
